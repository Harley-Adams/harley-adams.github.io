import { useRecoilValue } from "recoil";
import { currentLobbyIdState } from "../WordleState";
import { WordlePlayerContract } from "../WordleContract";

export const UpdateWordleLobby = (update: WordlePlayerContract): void => {
  // if (!player) {
  //     return;ß
  // }
  // const lobbyId = useRecoilValue(currentLobbyIdState);
  // pubsub.UpdateLobby(
  //     player?.EntityToken,
  //     currentLobbyId,
  //     (updateResult) => {},
  //     undefined,
  //     update
  // );
};
